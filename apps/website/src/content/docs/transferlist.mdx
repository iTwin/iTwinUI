---
title: TransferList
description: The TransferList component is used to transfer items between two lists.
thumbnail: Menu
related:
  - list
---

<p>{frontmatter.description}</p>

The labels should always be provided above the TransferList.

The `TransferList.Item` renders a `ListItem` and is compatible with `ListItem`'s [subcomponents](list#with-subcomponents).

<LiveExample src='TransferList.main.jsx'>
  <AllExamples.TransferListMainExample client:load />
</LiveExample>

## Props

<PropsTable path='@itwin/itwinui-react/esm/core/TransferList/TransferList.d.ts' />
