---
title: Typography
description: Typography components provide consistency in text styling across the application.
thumbnail: Typography
---

<p>{frontmatter.description}</p>

## Text

The `Text` component is a polymorphic typography component that can be used to render any kind of text as any kind of element.

```jsx
<Text>This is a text.</Text>
```

### Variant

The `variant` prop can be used to determine which type of typography size should be utilized for the content of the application. The available variants are:

- `"headline"`: corresponds to an `<h1>` text.

- `"title"`: corresponds to an `<h2>` text.

- `"subheading"`: corresponds to an `<h3>` text.

- `"leading"`: corresponds to an `<h4>` text.

- `"body"` (default): corresponds to a `<p>` text.

- `"smaller"`: corresponds to a `<smaller>` text.

<LiveExample src='Text.variant.jsx'>
  <AllExamples.TextVariantExample client:load />
</LiveExample>

### Muted

To achieve a muted or desaturated text effect, the `isMuted` prop can be passed into the component. Muted text helps differentiating less important information as well as enhancing the overall readability.

<LiveExample src='Text.muted.jsx' truncate={false}>
  <AllExamples.TextMutedExample client:load />
</LiveExample>

### Skeleton

To indicate that content is loading and will be available soon, use skeleton text by setting the `isSkeleton` prop on the component.

The `isSkeleton` prop can be passed along with `variant` to show indicate different text section being loaded. By default or with no `variant` being passed, the skeleton has the size of a `"body"` text.

<LiveExample src='Text.skeleton.jsx'>
  <AllExamples.TextSkeletonExample client:load />
</LiveExample>

### Polymorphic

The `Text` component supports the polymorphic `as`, allowing end users to render the text as a specific heading level while maintaining a desired size. This provides users the flexibility to control both the visual style and underlying HTML element used for rendering the texts.

<LiveExample src='Text.polymorphic.jsx' truncate={false}>
  <AllExamples.TextPolymorphicExample client:load />
</LiveExample>

### Props

<PropsTable path='@itwin/itwinui-react/esm/core/Typography/Text.d.ts' />

## Label

The `Label` component is a fundamental element that provides descriptive text or captions for form inputs. It can be used with `Input` components by setting the `htmlFor` attribute to match the `id` of the corresponding input element.

This association enhances accessibility by ensuring that screen readers and other assistive technologies can correctly identify the relationship between the label and the input field.

<LiveExample src='Label.main.jsx'>
  <AllExamples.LabelMainExample client:load />
</LiveExample>

### Required

To create a required label, end users can pass the `required` prop into the `Label` to indicate the visual indicator (\*) the component.

<LiveExample src='Label.required.jsx'>
  <AllExamples.LabelRequiredExample client:load />
</LiveExample>

### Display style

There are two available display styles:

- `"block"` (default): places the label and the other fields on separate lines without adding any extra spacing.
- `"inline"`: displays the label inline with other elements by adding a right margin to it.

<LiveExample src='Label.inline.jsx'>
  <AllExamples.LabelInlineExample client:load />
</LiveExample>

### Polymorphic

The `as` prop allows the `Label` component to be rendered as a different element while maintaining its styling. This increases the reusability and versatility of the `Label` by removing the default behaviors of the HTML `label` element, such as navigating the focus to the input when clicked.

<LiveExample src='Label.polymorphic.jsx'>
  <AllExamples.LabelPolymorphicExample client:load />
</LiveExample>

### Props

<PropsTable path='@itwin/itwinui-react/esm/core/Label/Label.d.ts' />
