---
title: Toggle switch
description: The toggle switch is a high contrast component used for toggling settings that only have two states.
thumbnail: ToggleSwitch
---

<p>{frontmatter.description}</p>

<LiveExample src='ToggleSwitch.main.jsx' truncate={false}>
  <AllExamples.ToggleSwitchMainExample client:load />
</LiveExample>

Toggle switches are instant action components used to toggle an option instantly with a single click or tap. They are more common in mobile layouts, but are also present in desktop environments. For example, on mobile, you can go to the settings menu and enable or disable wi-fi, airplane mode and sound with a single tap using the associated toggle switch.

It fulfills a role similar to that of a [checkbox](checkbox), though they are not necessarily used in the same contexts. To learn more about when to use a toggle switch or a checkbox, please read [Checkbox vs Toggle Switch by Saadia Minhas](https://uxplanet.org/checkbox-vs-toggle-switch-7fc6e83f10b8).

## Rendering of wrapper

By default, `ToggleSwitch` renders a wrapper around the `<input>` element. If the `ToggleSwitch.noUnnecessaryWrapper` future flag is enabled, the wrapper is only rendered when `label` or `icon` (deprecated) is passed.

The conditional rendering behavior of the wrapper when the `ToggleSwitch.noUnnecessaryWrapper` future flag is enabled is consistent with the same behavior in `Checkbox` and `Radio` where the wrapper is rendered only when `label` is passed.

## Application of props

By default, `className` and `style` are applied on the wrapper element, and all other DOM props are passed to `<input>` element.

When `wrapperProps` is passed or when the `ToggleSwitch.consistentPropsSpread` future flag is enabled, `className` and `style` are applied on the `<input>` element, similar to other DOM props.

But when the wrapper is not rendered ([more info](#rendering-of-wrapper)), `className` and `style` are always applied on the `<input>` element.

Below are some examples of how props are applied in different scenarios:

Default:

```jsx
<ThemeProvider>
  <ToggleSwitch
    className='my-class' // applied to wrapper
    style={{ padding: 16 }} // applied to wrapper
    //
    // Other props are applied to input
    data-dummy='value' // applied to input
  />
</ThemeProvider>
```

`ThemeProvider`'s `ToggleSwitch.noUnnecessaryWrapper` is enabled:

```jsx
<ThemeProvider future={{ ToggleSwitch: { noUnnecessaryWrapper: true } }}>
  <ToggleSwitch
    className='my-class' // applied to input
    style={{ padding: 16 }} // applied to input
    //
    // Other props are applied to input
    data-dummy='value' // applied to input
  />
</ThemeProvider>
```

With `wrapperProps`:

```jsx
<ToggleSwitch
  className='my-class' // applied to input
  style={{ padding: 16 }} // applied to input
  wrapperProps={{
    className: 'my-wrapper-class', // applied to wrapper
    style: { padding: 16 }, // applied to wrapper
  }}
  //
  // Other props are applied to input
  data-dummy='value' // applied to input
/>
```

`ThemeProvider`'s `ToggleSwitch.consistentPropsSpread` is enabled:

```jsx
<ThemeProvider future={{ ToggleSwitch: { consistentPropsSpread: true } }}>
  <ToggleSwitch
    className='my-class' // applied to input
    style={{ padding: 16 }} // applied to input
    //
    // Other props are applied to input
    data-dummy='value' // applied to input
  />
</ThemeProvider>
```

## Usage

### Sizes

There are two sizes available.

<LiveExample src='ToggleSwitch.sizes.jsx'>
  <AllExamples.ToggleSwitchSizesExample client:load />
</LiveExample>

### Label placement

A toggle switch's label can be placed on the right (default), left, or not used at all although it is strongly recommended that a label should be used.

<LiveExample src='ToggleSwitch.labels.jsx'>
  <AllExamples.ToggleSwitchLabelsExample client:load />
</LiveExample>

### With input group

Toggle switches can be paired with the [input group](inputgroup) which gives many additional options.

<LiveExample src='ToggleSwitch.inputgroup.jsx'>
  <AllExamples.ToggleSwitchInputGroupExample client:load />
</LiveExample>

## Props

<PropsTable path='@itwin/itwinui-react/esm/core/ToggleSwitch/ToggleSwitch.d.ts' />
