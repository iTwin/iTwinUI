---
const isDev = import.meta.env.DEV;

const title = isDev ? 'iTwinUI (local)' : 'iTwinUI';
const faviconHref = isDev ? 'favicon-dev.svg' : 'favicon.svg';

const links = [
  {
    name: 'html pages (current)',
    href: isDev ? 'http://localhost:5173' : 'css',
  },
  {
    name: 'storybook (current)',
    href: isDev ? 'http://localhost:6006' : 'react',
  },
  {
    name: 'docs website (wip)',
    href: isDev ? 'http://localhost:1700' : 'https://itwinui.bentley.com',
  },
  ...(!isDev ? [{ name: 'next major version (v3)', href: 'dev' }] : []),
  ...(isDev
    ? [
        { name: 'vite playground', href: 'http://localhost:1701' },
        { name: 'next playground', href: 'http://localhost:1702' },
        { name: 'astro playground', href: 'http://localhost:1703' },
      ]
    : []),
];
---

<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <link rel='icon' type='image/svg+xml' href={faviconHref} />
    <meta name='viewport' content='width=device-width' />
    <title>{title}</title>

    <style is:global>
      @import '@itwin/itwinui-variables';
      @import '@itwin/itwinui-variables/os.css';
      @import '@itwin/itwinui-css';
    </style>
  </head>

  <body class='iui-root'>
    <h1 class='iui-text-headline'>{title}</h1>

    <ul>
      {
        links.map(({ name, href }) => (
          <li>
            <a class='iui-anchor' href={href}>
              {name}
            </a>
          </li>
        ))
      }
    </ul>

    <style>
      body {
        padding: 1.5rem;
        font-size: 1rem;
      }
    </style>
  </body>
</html>
