---
'@itwin/itwinui-react': minor
---

Replaced several instances of `SvgChevronRight` with `SvgChevronRightSmall` for better visual balance. Components affected: `Breadcrumbs`, `ExpandableBlock`, `Table` (row expanders), and `TreeNode`.
