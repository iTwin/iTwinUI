{
  "name": "a11y",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "docker run --rm --entrypoint /bin/bash -v $PWD:/iui -w /iui  cypress/included:13.2.0 -c \"yarn workspace a11y test:run\"",
    "test:run": "cypress run --component",
    "open": "cypress open --component",
    "clean": "rimraf .turbo && rimraf node_modules"
  },
  "dependencies": {
    "@itwin/itwinui-react": "*",
    "axe-core": "^4.8.2",
    "cypress": "13.2.0",
    "cypress-axe": "^1.5.0",
    "examples": "*"
  }
}
