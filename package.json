{
  "name": "@bentley/itwinui",
  "version": "0.3.0",
  "author": "Bentley Systems",
  "license": "MIT",
  "main": "src/index.scss",
  "description": "CSS (Sass/SCSS) library for building beautiful and well working web UI components within Bentley Systems & iTwin.js applications.",
  "homepage": "https://bentleycs.visualstudio.com/UX%20Design/_git/iTwinUI",
  "url": "https://dev.azure.com/bentleycs/UX%20Design/_boards/board/t/iTwinUI/Backlog%20items",
  "email": "3b7617b6.bentley.onmicrosoft.com@amer.teams.ms",
  "keywords": [
    "css",
    "sass",
    "scss",
    "html",
    "react",
    "ui",
    "ux",
    "standards",
    "theme",
    "light",
    "dark",
    "bentley systems",
    "itwin"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "yarn lint"
    }
  },
  "scripts": {
    "build": "yarn clean && yarn build:scss && yarn build:css && yarn build:jss && yarn build:html && yarn copy-files",
    "build:scss": "cpx \"./src/**/*.scss\" ./lib/scss",
    "build:css": "tsc ./scripts/src/compile.ts --outDir ./scripts/ && node ./scripts/compile.js",
    "build:jss": "tsc ./scripts/src/makeJss.ts --outDir ./scripts && node ./scripts/makeJss.js lib/css lib/jss",
    "build:html": "html-minifier --input-dir ./backstop/tests --output-dir ./backstop/minified --remove-comments --collapse-whitespace --collapse-inline-tag-whitespace",
    "build:watch": "yarn build && chokidar \"{src,scripts/src,backstop/tests}/**/*\" -c \"yarn build:scss && yarn build:css && yarn build:jss && yarn build:html\"",
    "copy-files": "cpx package.json lib && cpx README.md lib",
    "clean": "rimraf ./lib ./backstop/results/bitmaps_test",
    "lint": "node ./scripts/lint.js",
    "approve": "backstop --config=./backstop/backstop.js approve",
    "test": "yarn build && yarn test:ci",
    "test:ci": "backstop --config=./backstop/backstop.js test --docker",
    "bump": "bump --prompt --all --commit --no-verify"
  },
  "devDependencies": {
    "backstopjs": "^5.1.0",
    "chokidar-cli": "^1.2.2",
    "compare-versions": "^3.4.0",
    "cpx": "^1.5.0",
    "html-minifier": "^4.0.0",
    "husky": "^1.3.1",
    "jss-cli": "^5.0.2",
    "log-symbols": "^2.2.0",
    "node-sass": "^4.13.0",
    "rimraf": "^2.6.2",
    "run-func": "^1.0.2",
    "stylelint": "^13.10.0",
    "stylelint-config-sass-guidelines": "^8.0.0",
    "typescript": "^3.3.3333",
    "version-bump-prompt": "^4.3.0"
  }
}
